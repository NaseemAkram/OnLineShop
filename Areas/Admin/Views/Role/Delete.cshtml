@{
    ViewData["Title"] = "Delete Roles";
}
<h1 class="text-info">Delete Role</h1>
<br />
<div>
    <form method="post" action="Delete" id="role-form" enctype="multipart/form-data">
        <div class="p-4 rounded-circle">
            <div asp-validation-summary="ModelOnly" class="text-danger">
            </div>

            <h3 class="text-danger">Do you really want to delete this user role</h3>

            <div class="form-group row">
                <div class="col-2">
                    <label>Role Name</label>
                </div>
                <div>
                    <input type="hidden" value="@ViewBag.id" name="id" />
                </div>
                <div>
                    <input id="name" name="name" readonly  required value="@ViewBag.name" class="form-control" />
                </div>

            </div>

            <br />
            <div class="form-group">
                <input type="submit" class="btn btn-primary" value="Delete" />
                <a class="btn btn-dark" asp-action="Index">Back</a>
            </div>

        </div>
    </form>
</div>


@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>

        $(document).ready(function () {

            $("#user-form").validate({
                rules: {
                    UserName: {
                        required: true,
                    },

                    PasswordHash: {
                        required: true,
                    },

                    messages: {
                        UserName: "Please enter user name",
                        PasswordHash: "Please enter password"
                    }
                }
            });

        });

    </script>

    }





